"""
A lightweight, flexible Object-Relational Mapping (ORM) for MongoDB in Python.

This setup.py file is provided for backward compatibility with older pip versions.
Modern installations should use pyproject.toml for dependency management.
"""

from setuptools import setup

if __name__ == "__main__":
    setup()
